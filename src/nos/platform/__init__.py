"""
nOS platform layer: Flask app, REST API, Socket.IO, SQLAlchemy services, web UI.

Requires optional dependencies ``nos[web]``. Import this package only when running
or deploying the full server — not needed for library-only use of :mod:`nos.core`.
"""
